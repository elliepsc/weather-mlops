name: CD — Deploy (disabled)

# Disabled until deployment target is configured (Streamlit Cloud / VPS / etc.)
# Re-enable by replacing `workflow_dispatch` with the appropriate trigger:
#
#   on:
#     workflow_run:
#       workflows: ["CI — Weather Pipeline"]
#       types: [completed]
#
on:
  workflow_dispatch:   # manual trigger only — does nothing automatically

jobs:
  deploy:
    runs-on: ubuntu-latest
    # Uncomment and configure when a deployment target is ready
    if: false
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "Configure deployment target first (Streamlit Cloud, VPS, etc.)"
