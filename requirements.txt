# Core data
pandas==2.1.4
numpy==1.26.4
scipy==1.14.1

# ML
xgboost==3.2.0
scikit-learn==1.5.2

# Experiment tracking
mlflow==3.11.1

# API
fastapi==0.136.0
uvicorn[standard]==0.45.0
prometheus-fastapi-instrumentator==7.1.0

# HTTP client (Open-Meteo)
requests==2.32.3

# Airflow (install separately — heavyweight, not included in pip install -r)
# On WSL: cd ~ && pip install apache-airflow --no-cache-dir
# On Linux/macOS: pip install apache-airflow==3.0.0

# Dev / testing
pytest==9.0.3
httpx==0.28.1
ruff==0.15.12
black==24.4.2

# Config management
pydantic==2.13.3
pydantic-settings==2.14.0
PyYAML==6.0.3

# Airflow image compatibility
cffi==1.17.1
cryptography==44.0.3
